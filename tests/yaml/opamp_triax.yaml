config:
  fontsize: 10

line1:
  - elements
  - Line: [{ lftlabel: "V", l: 0.6 }] # unit/5

smu:
  - elements
  - Opamp: [{ sign: false, anchor: in2 }]
  - label: [
        "SMU",
        { ofst: [-0.4, 0], loc: center, halign: center, valign: center },
      ] # align tuple might fail again? use halign/valign

line2:
  - elements
  - Line: [{ xy: "smu.out", l: 0.6 }]

push1:
  - drawing_state
  - push

line3:
  - elements
  - Line: [{ l: 0.75 }] # unit/4

triax:
  - elements
  - Triax: [{ length: 5, shieldofststart: 0.75 }]

pop1:
  - drawing_state
  - pop

dot1:
  - elements
  - Dot

res1:
  - elements
  - Resistor: [{ d: u, l: 3, zoom: 0.6 }]

line4:
  - elements
  - Line: [{ d: l }]

dot2:
  - elements
  - Dot

push2:
  - drawing_state
  - push

line5:
  - elements
  - Line: [{ d: d, toy: "smu.in1" }]

line6:
  - elements
  - Line: [{ d: r, tox: "smu.in1" }]

pop2:
  - drawing_state
  - pop

line7:
  - elements
  - Line: [{ d: u, l: 0.6 }]

line8:
  - elements
  - Line: [{ d: r, l: 0.6 }]

buf:
  - elements
  - Opamp: [{ sign: false, zoom: 0.6, anchor: in2 }]
  - label:
      ["BUF", { ofst: [-0.4, 0], loc: center, halign: center, valign: center }]

line9:
  - elements
  - Line: [{ d: l, xy: "buf.in1", l: 0.6 }]

line10:
  - elements
  - Line: [{ d: u, l: 0.6 }]

line11:
  - elements
  - Line: [{ d: r }]

line12:
  - elements
  - Line: [{ d: d, toy: "buf.out" }]

dot3:
  - elements
  - Dot

push3:
  - drawing_state
  - push

line13:
  - elements
  - Line: [{ d: l, tox: "buf.out" }]

pop3:
  - drawing_state
  - pop

line14:
  - elements
  - Line: [{ d: r, tox: "triax.guardstart_top" }]

line15:
  - elements
  - Line: [{ d: d, toy: "triax.guardstart_top" }]

gnd1:
  - elements
  - GroundChassis: [{ xy: "triax.shieldcenter" }]
