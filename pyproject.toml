[project]
name = "yaml-to-schemdraw"
version = "0.1.0"
description = "Generate schemdraw diagrams from YAML files or Python dictionaries."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["ruamel-yaml", "schemdraw"]

[dependency-groups]
dev = ["pylint", "pytest"]
